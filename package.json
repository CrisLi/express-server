{
  "name": "express-server",
  "version": "1.0.0",
  "description": "A very simple express server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "docker:build": "docker build -t crisli/express-server .",
    "docker:push": "docker push crisli/express-server",
    "start": "node server.js"
  },
  "author": "Chris Li",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.1",
    "express-history-api-fallback": "^2.1.0",
    "morgan": "^1.8.1"
  },
  "devDependencies": {
    "eslint": "^3.16.0",
    "eslint-config-airbnb-base": "^11.1.0",
    "eslint-plugin-import": "^2.2.0"
  }
}
